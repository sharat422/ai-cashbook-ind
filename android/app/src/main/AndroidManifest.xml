<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Biometric app-unlock (BiometricPrompt) — react-native-keychain -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Read bank transaction SMS to suggest entries (user-confirmed, on-device).
         RESTRICTED by Google Play: shipping this needs a Permissions Declaration
         exception (financial-SMS use case). Without it, keep the paste-only flow
         and remove this line. See docs/SMS_IMPORT.md. -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Camera capture for the receipt scanner / attachment flows -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone for multilingual voice transaction entry -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Reading images from the gallery (image picker). Android 13+ uses the
         granular media permission; older versions fall back to storage read. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Saving generated statement PDFs / CSVs to shared storage on older OSes -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- The device may or may not have a camera; don't block install on it -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Allow resolving the camera app for image capture intents (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
